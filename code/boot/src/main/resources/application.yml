server:
  port: 8081

spring:
  profiles:
    active: local
  datasource:
    url:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto:
    properties:
      hibernate:
        dialect:

sandbox:
  property: "Hola Mundo!"
